{plugins, [rebar3_path_deps]}.

{erl_opts, [debug_info, {i, "src/generated"}]}.
{src_dirs, [{"src", [{recursive, true}]}]}.

{deps, [
]}.

{profiles, [
    {test, [
        {erl_opts, [debug_info, nowarn_export_all, nowarn_unused_vars, {i, "src/generated"}]}
    ]}
]}.

{eunit_opts, [verbose]}.
